File: internal/ui/dashboard.go
Line: None
Diff: @@ -53,12 +59,24 @@ func (m DashboardModel) Init() tea.Cmd { return nil }
 
 func (m *DashboardModel) SetData(data AnalysisResult) {
 	m.data = data
+	// Precalculate heavy charts
+	activity := analyzer.CommitsPerDay(m.data.Commits)
+	m.precalcOverviewChart = RenderCommitActivity(activity, 15)
+	m.precalcActivityChart = RenderCommitActivity(activity, 40)
Body: _⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_

**Don't cache themed chart output here.**

`RenderCommitActivity` returns fully styled text, so caching that string in `SetData` freezes the old ANSI colors. After `t` switches the theme, the surrounding UI updates but the overview/activity charts stay on the previous theme until data is reloaded. Cache the raw activity data instead, or rebuild these cached strings when the theme changes.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@internal/ui/dashboard.go` around lines 62 - 65, The code is caching fully
styled chart strings (m.precalcOverviewChart / m.precalcActivityChart) by
calling RenderCommitActivity in SetData, which freezes ANSI colors when the
theme changes; instead, store the raw activity data returned by
analyzer.CommitsPerDay (or the activity slice) on the dashboard struct (e.g.,
add fields like m.precalcOverviewActivity and m.precalcActivityActivity) and
remove calls to RenderCommitActivity from SetData; call RenderCommitActivity
only at render time or when the theme changes (e.g., in the Draw/theme-change
handler) so charts are re-rendered with the current theme.
```

</details>

<!-- fingerprinting:phantom:medusa:grasshopper -->

<!-- This is an auto-generated comment by CodeRabbit -->
========================================
File: internal/ui/dashboard.go
Line: None
Diff: @@ -774,45 +796,55 @@ func (m DashboardModel) riskAlertsView() string {
 	)
 }
 
-func (m DashboardModel) apiStatusView() string {
-	header := TitleStyle.Render(" API Status ")
+type fetchedAPIStatusMsg string
 
-	client := github.NewClient()
-	rateLimit, err := client.GetRateLimit()
+func fetchAPIStatusCmd() tea.Cmd {
+	return func() tea.Msg {
+		client := github.NewClient()
+		rateLimit, err := client.GetRateLimit()
 
-	var rateLimitInfo string
-	if err != nil {
-		rateLimitInfo = "⚠️ Could not fetch rate limit info"
-	} else {
-		status := rateLimit.GetRateLimitStatus()
-		resetTime := rateLimit.FormatResetTime()
-		usage := rateLimit.UsagePercent()
-
-		rateLimitInfo = fmt.Sprintf(
-			"Status:    %s\n"+
-				"Remaining: %d / %d\n"+
-				"Used:      %.1f%%\n"+
-				"Reset:     %s",
-			status,
-			rateLimit.Resources.Core.Limit-rateLimit.Resources.Core.Remaining,
-			rateLimit.Resources.Core.Limit,
-			usage,
-			resetTime,
-		)
-	}
+		var rateLimitInfo string
+		if err != nil {
+			rateLimitInfo = "⚠️ Could not fetch rate limit info"
+		} else {
+			status := rateLimit.GetRateLimitStatus()
+			resetTime := rateLimit.FormatResetTime()
+			usage := rateLimit.UsagePercent()
+
+			rateLimitInfo = fmt.Sprintf(
+				"Status:    %s\n"+
+					"Remaining: %d / %d\n"+
+					"Used:      %.1f%%\n"+
+					"Reset:     %s",
+				status,
+				rateLimit.Resources.Core.Limit-rateLimit.Resources.Core.Remaining,
+				rateLimit.Resources.Core.Limit,
+				usage,
+				resetTime,
Body: _⚠️ Potential issue_ | _🟠 Major_ | _⚡ Quick win_

**The `Remaining` line is reporting used requests.**

`rateLimit.Resources.Core.Limit - rateLimit.Resources.Core.Remaining` is the consumed count, not the remaining count. This makes the API Status panel invert the most important number it shows.



<details>
<summary>Proposed fix</summary>

```diff
 			rateLimitInfo = fmt.Sprintf(
 				"Status:    %s\n"+
 					"Remaining: %d / %d\n"+
 					"Used:      %.1f%%\n"+
 					"Reset:     %s",
 				status,
-				rateLimit.Resources.Core.Limit-rateLimit.Resources.Core.Remaining,
+				rateLimit.Resources.Core.Remaining,
 				rateLimit.Resources.Core.Limit,
 				usage,
 				resetTime,
 			)
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
			rateLimitInfo = fmt.Sprintf(
				"Status:    %s\n"+
					"Remaining: %d / %d\n"+
					"Used:      %.1f%%\n"+
					"Reset:     %s",
				status,
				rateLimit.Resources.Core.Remaining,
				rateLimit.Resources.Core.Limit,
				usage,
				resetTime,
			)
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@internal/ui/dashboard.go` around lines 814 - 823, The Remaining value in the
rateLimitInfo string is currently using rateLimit.Resources.Core.Limit -
rateLimit.Resources.Core.Remaining (which is the consumed count); change it to
use rateLimit.Resources.Core.Remaining directly so the "Remaining" line reports
the actual remaining requests; update the fmt.Sprintf call that constructs
rateLimitInfo (the block referencing rateLimit.Resources.Core.Limit and
rateLimit.Resources.Core.Remaining) to pass rateLimit.Resources.Core.Remaining
for the Remaining placeholder.
```

</details>

<!-- fingerprinting:phantom:medusa:grasshopper -->

<!-- This is an auto-generated comment by CodeRabbit -->
========================================
File: internal/ui/themes.go
Line: None
Diff: @@ -162,26 +162,41 @@ func ApplyTheme(theme Theme) {
 
 	TitleStyle = lipgloss.NewStyle().
 		Bold(true).
-		Foreground(theme.Primary)
+		Padding(0, 2).
+		Foreground(theme.Background).
+		Background(theme.Primary).
+		MarginBottom(1)
 
 	BoxStyle = lipgloss.NewStyle().
 		Border(lipgloss.RoundedBorder()).
 		BorderForeground(theme.Border).
 		Padding(1, 4)
Body: _⚠️ Potential issue_ | _🟡 Minor_ | _⚡ Quick win_

**BoxStyle padding inconsistency with base definition.**

The `Padding(1, 4)` here doesn't match the base definition in `styles.go` line 21, which specifies `Padding(1, 3)`. Since `ApplyTheme` is called during initialization, this themed version will be used, but the inconsistency creates confusion and maintenance burden.




<details>
<summary>🔧 Align padding with base definition</summary>

```diff
 BoxStyle = lipgloss.NewStyle().
 	Border(lipgloss.RoundedBorder()).
 	BorderForeground(theme.Border).
-	Padding(1, 4)
+	Padding(1, 3)
```

Alternatively, if `(1, 4)` is the desired padding, update the base definition in `styles.go` line 21.
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
	BoxStyle = lipgloss.NewStyle().
		Border(lipgloss.RoundedBorder()).
		BorderForeground(theme.Border).
		Padding(1, 3)
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@internal/ui/themes.go` around lines 170 - 173, BoxStyle in themes.go is using
Padding(1, 4) which diverges from the base style padding defined in styles.go
(Padding(1, 3)), causing an inconsistency when ApplyTheme runs; to fix, make the
padding values consistent by changing BoxStyle's Padding to (1, 3) to match the
base, or if the themed value is intentional, update the base padding in
styles.go to (1, 4); update the BoxStyle definition (and/or the base style in
styles.go) and run the UI to verify spacing remains correct.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->
========================================
